// @flow
import React, {PureComponent} from 'react';

type IProps = {};

type IState = {};

export default class Player extends PureComponent<IProps, IState> {
  render() {
    return (
      <div id="player-wrapper"/>
    );
  }
}
